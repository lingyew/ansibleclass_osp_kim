- name: Delete the 3-tier instances on OSP
  hosts: jumpbox
  gather_facts: false
#  vars_files:
#    - ./3tier_var.file 

  tasks:
    - os_server:
        cloud: ospcloud
        server: app1
        state: absent
      register: deleteosp